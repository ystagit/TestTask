package elements.container;

import models.ModelEvent;
import shared.size.Size;
import shared.Position;
import views.GuiView;

class ContainerView extends GuiView {
    
    public function new(container:Container) {
        super(container);
    }

}